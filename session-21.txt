prjaasffzfxcufpe

Disadvantages
=============
1. no idempotency
2. No error handling
3. Little complex
4. Large scale servers handling is tough
5. Distribution specific, homogenous

connect one-server to another server
======================================
	ssh ec2-user@ip address touch shell.txt and give password (connect server)
	scp mongo.repo mongodb.sh ec2-user@172.31.26.110:/tmp (copy files)
	ssh ec2-user@ip_address 'cd/tmp && sudo sh mongodb.sh' (execute file)
1. copy mongo repo, mongodb.sh
2. execute them

Configuration management
======================
provisioning server -> baseline it according to our needs

1. Installing programming language
2. Creating system users and folders
3. Installing some system packages
4. Downloading code
5. Installing dependencies
6. Creating systemctl services
7. Enable and starting the services

CRUD

Push vs Pull
============
HYD -> Delhi

1. Person in Delhi go to DTDC office everyday and check for letter
2. Person stays home, DTDC delivers letter whenever it arrive

-------------------------------------
sudo dnf install ansible -y
If you need to connect any server, We must follow Below steps:
	protocol(ssh)
	port	(8080)
	username
	password
	IP
-------------------------------------
** connect to the remote server **
	ansible all -i 172.31.26.110,-e ansible_user=ec2-user -e ansible_password=DevOps321 -m ping
	[ec2-user@ip-172-31-27-151~]$ ansible all -i
		172.31.26.110, -e
		ansible_user=ec2-user -e
		ansible_password=DevOps321 -b-m dnf -a "name=nginx state=installed"
	systemctl enable nginx
	systemctl start nginx

-m module, -e extra, -b become root,

Linux commands == Ansible modules/collections

Linux Server STATE-1 -> STATE change STATE-2

YAML -> Yet another markup language

DTO -> data transfer objects
============================

name -> sivakumar
acc no
amount
date
branch
denomination

forms and templates

Markup languages
================
HTML, XML

<Register>
	<Name>Sivakumar</Name>
	<Mobile>12345678</Mobile>
	<Email>info@joindevops.com</Email>
</Register>

JSON

YAML


