If you get an error in a project, means you need to check the 3 things. 
    1. is a security group ok
    2. is Backend connect To the database. 
    3. is data is loaded into the database or not
    4. dns
I want to delete the old log files automatically, At midnight.
     So I need to write the Script(want that script) that will run automatically.
     Delete the old log files >>>see in next session 20
delete old logs
backup
disk utilisation and trigger email


1. which directory
2. is the directory exist?
3. find the files *.log older than 14 days
4. remove them

log the script execution

find . -name "*.log" -mtime +14